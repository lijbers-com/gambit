{
  "framework": "nextjs",
  "buildCommand": "npm run build && npm run build-storybook && mkdir -p public/storybook && cp -r storybook-static/* public/storybook/",
  "rewrites": [
    {
      "source": "/storybook",
      "destination": "/storybook/index.html"
    },
    {
      "source": "/storybook/:path*",
      "destination": "/storybook/:path*"
    }
  ]
}