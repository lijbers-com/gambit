{
  "buildCommand": "npm run build && mkdir -p .storybook-temp && npx storybook build -o .storybook-temp && rm -rf public/storybook && mv .storybook-temp public/storybook",
  "installCommand": "npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/storybook",
      "destination": "/storybook/index.html"
    },
    {
      "source": "/storybook/:path*",
      "destination": "/storybook/:path*"
    }
  ]
}