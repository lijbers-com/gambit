import{u as fe,r as n,j as e,M as be}from"./iframe-C5bSqRdg.js";import{A as pe,g as ve}from"./theme-navigation-17KOncgO.js";import{C as B,a as N,d as A,e as I,b as M,f as ye,g as Se,h as Ce}from"./card-D9J10WQk.js";import{M as Ne}from"./metric-row-C5Lnk1YE.js";import{B as h}from"./button-CMihJ9pV.js";import{I as D}from"./input-BbKIeO3x.js";import{S as Ee}from"./search-input-CF4V8rQz.js";import{L as w}from"./label-DRSji8LQ.js";import{B as Q}from"./badge-DpORWxNf.js";import{S as le}from"./switch-D4meuFbV.js";import{D as je}from"./date-picker-BSwb8brm.js";import{c as b}from"./utils-CBfrqCZ4.js";import{M as $e,L as Fe,a as Le}from"./monitor-speaker-CnbWk3Lx.js";import{S as ze}from"./store-DA6NizPB.js";import{G as De}from"./globe-DH-JFNDC.js";import{S as Oe}from"./scan-barcode-CtR_w5Lx.js";import{X as Ve}from"./x-Xece35A4.js";import{U as Ge}from"./users-Bz_43eCx.js";import{C as ce}from"./check-BliW7QYp.js";import{S as me}from"./sparkles-DXt8FadX.js";import{T as He}from"./trending-up-Ct__SSRy.js";import{D as Ke}from"./dollar-sign-BYVArxGe.js";import{F as Re,S as We}from"./render-icon-C5J9mvsf.js";import{E as Ue}from"./eye-k8OiKrgc.js";import{B as Je,H as Ye}from"./heart-CngNMBNI.js";import"./preload-helper-PPVm8Dsz.js";import"./side-navigation-BTABV-WY.js";import"./use-menu-B5WFxXUD.js";import"./chevron-down-BH3g__jq.js";import"./createLucideIcon-CkgRO_Hx.js";import"./chevron-right-zSdNciVz.js";import"./dropdown-menu-BPEWRI36.js";import"./index-DD08fZIQ.js";import"./index-DAh0e16s.js";import"./index-Z2B8irzh.js";import"./index-gOg22guj.js";import"./index-DJEyZNpx.js";import"./index-D5TxnOXk.js";import"./index-BDa5FaKX.js";import"./index-4SfGeEia.js";import"./Combination-BPLtMwXH.js";import"./index-DELDvHLi.js";import"./index-C48yRfEQ.js";import"./index-7AEp8Dl6.js";import"./checkbox-DTG8_xx0.js";import"./index-squPBcRJ.js";import"./circle-xaoSWTc6.js";import"./logo-CzbqsAtz.js";import"./smart-breadcrumbs-xQ-yQEDu.js";import"./breadcrumb-DCylyj0S.js";import"./default-routes-CqolUEBP.js";import"./page-header-BE690Ecl.js";import"./building-2-R1bhGwAK.js";import"./settings-2-BO2bzVa0.js";import"./ellipsis-A7mo7Ea1.js";import"./square-pen-CqwDJzpj.js";import"./settings-BhjJRHSG.js";import"./header-actions-BMmlE090.js";import"./popover-CtRsAG4M.js";import"./search-BEI7_bcP.js";import"./user-C-TKlPDM.js";import"./CategoricalChart-CHwgL6wo.js";import"./index-CCZnHES1.js";import"./LineChart-BiKNmC_Q.js";import"./ActivePoints-B42lPFi4.js";import"./LabelList-CcglBYb3.js";import"./ErrorBar-DUU192wj.js";import"./CartesianChart-CKaA2JcV.js";import"./dialog-DJcSVB7a.js";import"./index-B6oA7Lll.js";import"./plus-cm3zeQZS.js";import"./index-CdJFUDDL.js";import"./chevron-left-DOa0lkhI.js";const fa={title:"Page templates/Create Media Experience",component:pe,parameters:{layout:"fullscreen",docs:{description:{component:`
# Create Media Experience - Wizard

A step-by-step wizard for creating new media experiences.

## Steps

1. **Campaign Setup** - Enter the campaign name and select the brand to advertise for
2. **Campaign Goal** - Select the objective of the media experience
3. **Targeting** - Select audience segments and add targeting tags
4. **Budget & Schedule** - Set the campaign budget and date range
5. Review & Launch (future)
        `}}},tags:["autodocs"]},qe=({icon:V,title:_,description:G,selected:u,onClick:m})=>e.jsxs("button",{type:"button",onClick:m,className:`flex flex-col items-start text-left p-6 rounded-lg border-2 transition-all cursor-pointer hover:border-primary/50 hover:shadow-sm h-full ${u?"border-primary bg-primary/5 shadow-sm":"border-border bg-card"}`,children:[e.jsx("div",{className:`mb-4 ${u?"text-primary":"text-muted-foreground"}`,children:V}),e.jsx("h3",{className:"font-semibold text-sm mb-1",children:_}),e.jsx("p",{className:"text-xs text-muted-foreground leading-relaxed",children:G})]}),he=[{id:"awareness",icon:e.jsx(Ue,{size:24}),title:"Awareness",description:"Reach a broad audience and make them aware of your brand, product or service"},{id:"consideration",icon:e.jsx(Je,{size:24}),title:"Consideration",description:"Encourage people to think about your brand and seek out more information"},{id:"purchase",icon:e.jsx(We,{size:24}),title:"Purchase",description:"Drive sales and conversions on your website, in your app or in physical stores"},{id:"loyalty",icon:e.jsx(Ye,{size:24}),title:"Loyalty",description:"Strengthen existing customer relationships and drive repeat purchases"}],X=[{label:"Coca-Cola",value:"coca-cola"},{label:"Unilever",value:"unilever"},{label:"Procter & Gamble",value:"procter-gamble"},{label:"Nestlé",value:"nestle"},{label:"PepsiCo",value:"pepsico"},{label:"Heineken",value:"heineken"}],xe=[{id:"all-shoppers",label:"All Shoppers",description:"All active shoppers in the platform",reach:"12.4M"},{id:"frequent-buyers",label:"Frequent Buyers",description:"Customers with 3+ purchases per month",reach:"3.2M"},{id:"new-customers",label:"New Customers",description:"First-time visitors in the last 30 days",reach:"1.8M"},{id:"high-value",label:"High Value Customers",description:"Top 20% by average order value",reach:"2.1M"},{id:"lapsed",label:"Lapsed Customers",description:"No purchase in the last 90 days",reach:"4.6M"},{id:"category-buyers",label:"Category Buyers",description:"Active in relevant product categories",reach:"5.3M"}],ue=[{id:"606983",name:"Coca-Cola - coca-cola zero fl - 1 liter"},{id:"607124",name:"Pepsi - pepsi max - 1.5 liter"},{id:"608456",name:"Red Bull - energy drink original - 250ml"},{id:"609782",name:"Heineken - premium lager beer - 6x330ml"},{id:"610394",name:"Samsung - galaxy s24 ultra - 256GB"},{id:"611205",name:"iPhone - 15 pro max - 512GB"},{id:"612816",name:"Nike - air max 270 - size 42"},{id:"613427",name:"Adidas - ultraboost 22 - size 43"},{id:"614038",name:"Nutella - hazelnut spread - 750g"},{id:"615649",name:"Lay's - classic potato chips - 250g"},{id:"616250",name:"Dove - body wash sensitive - 500ml"},{id:"617861",name:"Nespresso - vertuo capsules - 30 pack"}],O=[{id:"display",name:"Display",description:"Banner ads across the retailer website and app",icon:$e,metrics:{reach:"3.2M",roas:"2.8x",sales:"€8,400",roasChange:"+8%"},aiPreset:{id:"display-ai",name:"AI optimised display campaign",description:"Automatically configured banner placements across homepage, category pages and checkout flow based on your audience and budget settings",placements:13,estImpressions:"4.3M"}},{id:"sponsored-products",name:"Sponsored Products",description:"Promoted product listings in search and category results",icon:Fe,metrics:{reach:"4.8M",roas:"4.2x",sales:"€12,600",roasChange:"+18%"},aiPreset:{id:"sp-ai",name:"AI optimised sponsored products",description:"Intelligent product placement in search results and category pages, optimised for your target audience and maximum return",placements:9,estImpressions:"5.0M"}},{id:"digital-instore",name:"Digital In-Store",description:"Digital screens and kiosks in physical retail locations",icon:Le,metrics:{reach:"680K",roas:"1.9x",sales:"€3,200",roasChange:"+5%"},aiPreset:{id:"dis-ai",name:"AI optimised in-store digital",description:"Smart selection of store entrance screens and aisle displays based on product category and shopper traffic patterns",placements:36,estImpressions:"770K"}},{id:"offline-instore",name:"Offline In-Store",description:"Physical media placements like shelf talkers, flyers and POS materials",icon:ze,metrics:{reach:"240K",roas:"1.4x",sales:"€1,800",roasChange:"+3%"},aiPreset:{id:"ois-ai",name:"AI optimised in-store media",description:"Optimal mix of shelf talkers and point-of-sale displays based on store performance data and product placement",placements:65,estImpressions:"275K"}},{id:"offsite",name:"Offsite",description:"Extend your campaign beyond the retailer with 3rd party display, socials, connected TV, DOOH, AI, audio and mailing",icon:De,metrics:{reach:"8.5M",roas:"2.1x",sales:"€15,200",roasChange:"+14%"},aiPreset:{id:"er-ai",name:"AI optimised offsite",description:"Multi-channel offsite advertising across 3rd party display, socials, connected TV, DOOH, AI, audio and mailing, matched to your retailer first-party data",placements:42,estImpressions:"9.8M"}}],Qe=[{id:"setup",label:"Campaign setup"},{id:"goal",label:"Campaign goal"},{id:"targeting",label:"Targeting"},{id:"budget",label:"Run time & budget"},{id:"review",label:"Media plan"}],Y={render:()=>{const{theme:V}=fe(),G=ve(V||"retailMedia"),[u,m]=n.useState(0),[j,Z]=n.useState(""),[T,ee]=n.useState(""),[v,te]=n.useState([]),[L,ae]=n.useState(""),[ge,z]=n.useState(!1),[l,se]=n.useState(null),[r,re]=n.useState([]),[we,ke]=n.useState([]),[H,K]=n.useState(""),[c,W]=n.useState(""),[p,g]=n.useState(void 0),[E,ie]=n.useState(!0),[a,i]=n.useState(()=>{const t={};return O.forEach(s=>{t[s.id]={mode:"preset",presetId:s.aiPreset.id}}),t}),d=he.find(t=>t.id===l),R=X.find(t=>t.value===T),$=j.trim()!==""&&T!=="",F=r.length>0,f=c.trim()!==""&&p?.from!==void 0&&p?.to!==void 0,y=t=>{re(s=>s.includes(t)?s.filter(o=>o!==t):[...s,t])},U=ue.filter(t=>t.name.toLowerCase().includes(L.toLowerCase())||t.id.includes(L)),oe=t=>{v.includes(t.id)||te([...v,t.id]),ae(""),z(!1)},ne=t=>{ae(t.target.value),z(t.target.value.length>0)},Pe=t=>{te(v.filter(s=>s!==t))};n.useEffect(()=>{const t=s=>{s.target.closest("[data-dropdown-container]")||z(!1)};return document.addEventListener("mousedown",t),()=>document.removeEventListener("mousedown",t)},[]);const Be=t=>{switch(t){case"setup":return $?`${j} · ${R?.label}`:null;case"goal":return d?d.title:null;case"targeting":{if(!F)return null;const s=r.length;return`${s} audience${s!==1?"s":""}`}case"budget":return f?`€${c} total`:null;case"review":{const s=Object.values(a).filter(Boolean).length;return s===0?null:`${s} proposition${s!==1?"s":""}`}default:return null}},Ae=(t,s)=>s<u?"completed":s===u?"active":"pending",de=n.useMemo(()=>{if(r.length===0)return null;let t=0;return r.forEach(o=>{const S=xe.find(k=>k.id===o);if(S){const k=parseFloat(S.reach.replace("M",""));t+=k}}),`${Math.min(t*.8,15).toFixed(1)}M`},[r]),x=n.useMemo(()=>{let t=0,s=0,o=0,S=0;return Object.entries(a).forEach(([k,J])=>{if(!J||(S++,J.mode==="empty"))return;const C=O.find(Te=>Te.id===k);if(!C)return;const P=C.metrics.reach;P.endsWith("M")?t+=parseFloat(P.replace("M","")):P.endsWith("K")&&(t+=parseFloat(P.replace("K",""))/1e3);const Ie=parseFloat(C.metrics.roasChange.replace("%","").replace("+",""));s+=Ie;const Me=parseFloat(C.metrics.sales.replace("€","").replace(",",""));o+=Me}),{additionalReach:t,roasBoost:s,additionalSales:o,selectedCount:S}},[a]);return e.jsx(be,{children:e.jsx(pe,{routes:G,logo:{src:"/next.svg",alt:"Logo",width:40,height:40},user:{name:"Jane Doe",avatar:"https://ui-avatars.com/api/?name=Jane+Doe&size=32"},onLogout:()=>alert("Logout clicked"),breadcrumbProps:{namespace:""},pageHeaderProps:{title:"Create media experience",subtitle:"",headerRight:null},children:e.jsxs("div",{className:"space-y-6",children:[e.jsx(Ne,{metrics:[{key:"reach",label:"Est. Reach",value:r.length>0?`${(parseFloat(de?.replace("M","")||"0")+x.additionalReach).toFixed(1)}M`:"-",subMetric:r.length>0?x.selectedCount>0?`${r.length} audience${r.length!==1?"s":""} · ${x.selectedCount} proposition${x.selectedCount!==1?"s":""}`:`${r.length} audience${r.length!==1?"s":""}`:"No audience selected",badgeValue:r.length>0&&x.additionalReach>0?`+${x.additionalReach.toFixed(1)}M`:r.length>1?"Combined":void 0,badgeVariant:x.additionalReach>0?"success":"info"},{key:"budget",label:"Budget",value:c.trim()!==""?`€${Number(c).toLocaleString()}`:"-",subMetric:c.trim()!==""?p?.from&&p?.to?`€${(parseFloat(c)/(Math.ceil((p.to.getTime()-p.from.getTime())/(1e3*60*60*24))+1)).toFixed(0)}/day`:"No dates set":"No budget set"},{key:"roas",label:"Est. ROAS",value:c.trim()!==""?`${((2.4+(parseFloat(c)>5e3?1.2:parseFloat(c)>2e3?.6:0)+(r.length>2?.5:0))*(1+x.roasBoost/100)).toFixed(1)}x`:"-",subMetric:c.trim()!==""?"Predicted return":"Set budget to calculate",badgeValue:c.trim()!==""?x.roasBoost>0?`+${x.roasBoost}%`:"+12%":void 0,badgeVariant:"success"},{key:"sales",label:"Est. Sales",value:c.trim()!==""?(()=>{const t=2.4+(parseFloat(c)>5e3?1.2:parseFloat(c)>2e3?.6:0)+(r.length>2?.5:0),o=parseFloat(c)*t+x.additionalSales;return`€${Math.round(o).toLocaleString()}`})():"-",subMetric:c.trim()!==""?"Projected revenue":"Set budget to calculate",badgeValue:c.trim()!==""?x.additionalSales>0?`+€${x.additionalSales.toLocaleString()}`:"Based on avg.":void 0,badgeVariant:x.additionalSales>0?"success":"secondary"}],selectedKeys:["reach","budget","roas","sales"],maxVisible:4,defaultVariant:"default",removable:!1}),e.jsxs("div",{className:"grid grid-cols-1 lg:grid-cols-3 gap-6",children:[e.jsxs("div",{className:"lg:col-span-2 min-w-0",children:[u===0&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsx(A,{className:"text-lg",children:"Campaign setup"}),e.jsx(I,{children:"Enter the basic details for your new media experience"})]}),e.jsxs(M,{children:[e.jsxs("div",{className:"space-y-6",children:[e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{htmlFor:"campaign-name",children:"Campaign name"}),e.jsx(D,{id:"campaign-name",placeholder:"e.g. Summer Sale 2026",value:j,onChange:t=>Z(t.target.value),hint:"Give your campaign a descriptive name to easily identify it later"})]}),e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{htmlFor:"brand",children:"Brand"}),e.jsx(D,{dropdown:!0,options:X,value:T,onChange:t=>ee(t),placeholder:"Select a brand"}),e.jsx("div",{className:"text-xs text-muted-foreground mt-1",children:"Choose the brand this campaign will advertise for"})]}),e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{children:"Retail products"}),e.jsxs("div",{className:"relative","data-dropdown-container":!0,children:[e.jsx(Ee,{value:L,onChange:ne,onClick:()=>z(!0),placeholder:"Select product by name or ID...",className:"w-full",icon:e.jsx(Oe,{className:"w-4 h-4"})}),ge&&e.jsx("div",{className:"absolute z-10 w-full mt-1 bg-background border rounded-md shadow-lg max-h-60 overflow-y-auto",children:U.length>0?U.map(t=>e.jsx("div",{className:`p-3 hover:bg-muted/50 cursor-pointer border-b last:border-b-0 ${v.includes(t.id)?"bg-primary/5":""}`,onClick:()=>oe(t),children:e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsx("span",{className:"text-sm",children:t.name}),e.jsxs("span",{className:"text-xs text-muted-foreground",children:["#",t.id]})]})},t.id)):e.jsx("div",{className:"p-3 text-sm text-muted-foreground",children:"No products found"})})]}),v.length>0&&e.jsx("div",{className:"flex flex-wrap gap-2 mt-2",children:v.map(t=>{const s=ue.find(o=>o.id===t);return s?e.jsxs(Q,{variant:"default",size:"large",className:"gap-1 pr-1.5",children:[s.name,e.jsx("button",{type:"button",onClick:()=>Pe(t),className:"ml-1 hover:text-destructive transition-colors rounded-full",children:e.jsx(Ve,{size:14})})]},t):null})}),e.jsx("div",{className:"text-xs text-muted-foreground",children:v.length>0?`${v.length} retail product${v.length>1?"s":""} selected`:"Search and select retail products to target for this campaign"})]})]}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",children:"Cancel"}),e.jsx(h,{disabled:!$,onClick:()=>m(1),children:"Continue"})]})]})]}),u===1&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsx(A,{className:"text-lg",children:"What is your campaign goal?"}),e.jsx(I,{children:"Select a goal to align the objectives and settings that work best for your campaign"})]}),e.jsxs(M,{children:[e.jsx("div",{className:"grid grid-cols-1 sm:grid-cols-2 gap-4",children:he.map(t=>e.jsx(qe,{icon:t.icon,title:t.title,description:t.description,selected:l===t.id,onClick:()=>se(t.id)},t.id))}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",onClick:()=>m(0),children:"Back"}),e.jsx(h,{disabled:!l,onClick:()=>m(2),children:"Continue"})]})]})]}),u===2&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsxs(A,{className:"text-lg flex items-center gap-2",children:[e.jsx(Ge,{size:20}),"Select your audience"]}),e.jsx(I,{children:"Choose one or more audience segments to target with your campaign"})]}),e.jsxs(M,{children:[e.jsx("div",{className:"space-y-3",children:xe.map(t=>{const s=r.includes(t.id);return e.jsxs("button",{type:"button",onClick:()=>y(t.id),className:`w-full flex items-start gap-3 p-4 rounded-lg border transition-all text-left ${s?"border-primary bg-primary/5":"border-border hover:border-primary/30"}`,children:[e.jsx("div",{className:b("mt-0.5 h-4 w-4 shrink-0 rounded-sm border border-primary flex items-center justify-center",s&&"bg-primary text-primary-foreground"),children:s&&e.jsx(ce,{className:"h-4 w-4"})}),e.jsxs("div",{className:"flex-1 min-w-0",children:[e.jsxs("div",{className:"flex items-center justify-between gap-2",children:[e.jsx("span",{className:"text-sm font-medium",children:t.label}),e.jsxs("span",{className:"text-xs text-muted-foreground flex-shrink-0",children:["Reach: ",t.reach]})]}),e.jsx("p",{className:"text-xs text-muted-foreground mt-0.5",children:t.description})]})]},t.id)})}),de&&e.jsx("div",{className:"mt-4 p-3 rounded-lg bg-primary/5 border border-primary/20",children:e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsx("span",{className:"text-sm font-medium",children:"Estimated reach"}),e.jsxs("span",{className:"text-sm font-semibold text-primary",children:[de," shoppers"]})]})}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",onClick:()=>m(1),children:"Back"}),e.jsx(h,{disabled:!F,onClick:()=>m(3),children:"Continue"})]})]})]}),u===3&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsx(A,{className:"text-lg",children:"Run time & budget"}),e.jsx(I,{children:"Set when your campaign runs and how much you want to spend"})]}),e.jsxs(M,{children:[e.jsxs("div",{className:"space-y-6",children:[e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{children:"Run time"}),e.jsx(je,{dateRange:p,onDateRangeChange:g,placeholder:"Select start and end date",showPresets:!0}),e.jsx("div",{className:"text-xs text-muted-foreground",children:"Your campaign will automatically start and stop on the selected dates"})]}),e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{htmlFor:"budget-amount",children:"Total budget"}),e.jsxs("div",{className:"relative",children:[e.jsx("span",{className:"absolute left-3 top-1/2 -translate-y-1/2 text-sm text-muted-foreground",children:"€"}),e.jsx(D,{id:"budget-amount",type:"number",placeholder:"e.g. 5000",value:c,onChange:t=>W(t.target.value),className:"pl-7"})]}),e.jsx("div",{className:"text-xs text-muted-foreground",children:"The maximum total amount for the entire campaign duration"})]}),c&&p?.from&&p?.to&&e.jsxs("div",{className:"p-3 rounded-lg bg-primary/5 border border-primary/20",children:[e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsx("span",{className:"text-sm font-medium",children:"Daily average"}),e.jsxs("span",{className:"text-sm font-semibold text-primary",children:["€",(parseFloat(c)/(Math.ceil((p.to.getTime()-p.from.getTime())/(1e3*60*60*24))+1)).toFixed(2),"/day"]})]}),e.jsxs("p",{className:"text-xs text-muted-foreground mt-1",children:["€",c," over ",Math.ceil((p.to.getTime()-p.from.getTime())/(1e3*60*60*24))+1," days"]})]}),e.jsxs("div",{className:b("rounded-lg border p-4 transition-all",E?"border-primary/30 bg-primary/5":"border-border"),children:[e.jsxs("div",{className:"flex items-start gap-3",children:[e.jsxs("div",{className:"flex-1 min-w-0",children:[e.jsxs("div",{className:"flex items-center gap-2",children:[e.jsx(me,{size:14,className:E?"text-primary":"text-muted-foreground"}),e.jsx("span",{className:"text-sm font-medium",children:"Auto budget optimization"}),E&&e.jsx(Q,{variant:"secondary",className:"text-[10px] px-1.5 py-0",children:"AI"})]}),e.jsx("p",{className:"text-xs text-muted-foreground mt-1",children:"Automatically distribute your budget across propositions based on real-time performance data to maximise ROAS"})]}),e.jsx(le,{checked:E,onCheckedChange:ie})]}),E&&c&&e.jsxs("div",{className:"mt-3 pt-3 border-t border-primary/20 flex gap-4",children:[e.jsxs("div",{className:"flex items-center gap-1.5",children:[e.jsx(He,{size:12,className:"text-primary"}),e.jsx("span",{className:"text-xs text-muted-foreground",children:"Avg. +18% ROAS improvement"})]}),e.jsxs("div",{className:"flex items-center gap-1.5",children:[e.jsx(Ke,{size:12,className:"text-primary"}),e.jsx("span",{className:"text-xs text-muted-foreground",children:"Real-time rebalancing"})]})]})]})]}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",onClick:()=>m(2),children:"Back"}),e.jsx(h,{disabled:!f,onClick:()=>m(4),children:"Continue"})]})]})]}),u===4&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsx(A,{className:"text-lg",children:"Media plan"}),e.jsx(I,{children:"Toggle propositions on or off and choose a preset campaign or start with an empty configuration."})]}),e.jsxs(M,{children:[e.jsx("div",{className:"space-y-4",children:O.map(t=>{const s=a[t.id],o=s!=null,S=s?.mode==="preset",k=s?.mode==="empty",J=t.icon;return e.jsxs("div",{className:b("rounded-lg border transition-all",o?"border-border":"border-border/50"),children:[e.jsxs("div",{className:"flex items-center gap-3 p-4",children:[e.jsx("div",{className:b("w-8 h-8 rounded-lg flex items-center justify-center transition-colors",o?"bg-primary text-primary-foreground":"bg-muted text-muted-foreground"),children:e.jsx(J,{size:16})}),e.jsxs("div",{className:"flex-1 min-w-0",children:[e.jsx("span",{className:b("text-sm font-semibold",!o&&"text-muted-foreground"),children:t.name}),e.jsx("p",{className:"text-xs text-muted-foreground",children:t.description})]}),e.jsxs("div",{className:"flex items-center gap-3 flex-shrink-0",children:[o&&e.jsx("span",{className:"text-xs text-muted-foreground",children:k?"– reach · – ROAS":`${t.metrics.reach} reach · ${t.metrics.roas} ROAS`}),e.jsx(le,{checked:o,onCheckedChange:C=>{i(C?P=>({...P,[t.id]:{mode:"preset",presetId:t.aiPreset.id}}):P=>({...P,[t.id]:null}))}})]})]}),o&&e.jsx("div",{className:"px-4 pb-4",children:e.jsxs("div",{className:"grid grid-cols-2 gap-3",children:[e.jsxs("button",{type:"button",onClick:()=>{i(C=>({...C,[t.id]:{mode:"preset",presetId:t.aiPreset.id}}))},className:b("flex flex-col items-start text-left p-4 rounded-lg border-2 transition-all cursor-pointer h-full",S?"border-primary bg-primary/5 shadow-sm":"border-border hover:border-primary/30"),children:[e.jsxs("div",{className:"flex items-center gap-2 mb-2",children:[e.jsx(me,{size:14,className:"text-primary flex-shrink-0"}),e.jsx("span",{className:"text-sm font-medium",children:"Automatic"}),e.jsx(Q,{variant:"secondary",className:"text-[10px] px-1.5 py-0",children:"AI"})]}),e.jsx("p",{className:"text-xs text-muted-foreground leading-relaxed mb-3",children:t.aiPreset.description}),e.jsxs("div",{className:"flex gap-3 mt-auto",children:[e.jsxs("span",{className:"text-xs text-muted-foreground",children:[t.aiPreset.placements," placements"]}),e.jsxs("span",{className:"text-xs text-muted-foreground",children:["~",t.aiPreset.estImpressions," imp."]})]})]}),e.jsxs("button",{type:"button",onClick:()=>{i(C=>({...C,[t.id]:{mode:"empty"}}))},className:b("flex flex-col items-start text-left p-4 rounded-lg border-2 border-dashed transition-all cursor-pointer h-full",k?"border-primary bg-primary/5 shadow-sm":"border-muted-foreground/20 hover:border-primary/30"),children:[e.jsxs("div",{className:"flex items-center gap-2 mb-2",children:[e.jsx(Re,{size:14,className:"text-muted-foreground flex-shrink-0"}),e.jsx("span",{className:"text-sm font-medium",children:"Empty campaign"})]}),e.jsxs("p",{className:"text-xs text-muted-foreground leading-relaxed",children:["Start with a blank ",t.name.toLowerCase()," campaign and configure manually"]})]})]})})]},t.id)})}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",onClick:()=>m(3),children:"Back"}),e.jsx(h,{onClick:()=>{const t=j||"New Media Experience";window.location.href=`/campaigns?new=${encodeURIComponent(t)}`},children:"Launch media experience"})]})]})]})]}),e.jsx("div",{className:"flex flex-col gap-4",children:e.jsxs(ye,{children:[e.jsx(N,{children:e.jsx(Se,{children:"Summary"})}),e.jsx(Ce,{children:e.jsxs("div",{className:"relative pl-12",children:[e.jsx("div",{className:"absolute left-[19px] top-[16px] bottom-[16px] w-px bg-border"}),e.jsx("div",{className:"space-y-4",children:Qe.map((t,s)=>{const o=Ae(t.id,s),S=Be(t.id);return e.jsxs("div",{className:"relative flex items-start -ml-12",children:[e.jsx("div",{className:"w-10 flex justify-center",children:e.jsx("div",{className:`w-8 h-8 rounded-full flex items-center justify-center text-xs font-medium ${o==="completed"?"bg-primary text-primary-foreground":o==="active"?"bg-background text-primary border-2 border-primary":"bg-background text-muted-foreground border border-border"}`,children:o==="completed"?e.jsx(ce,{size:14}):s+1})}),e.jsxs("div",{className:"ml-3 flex-1 min-w-0 pt-1",children:[e.jsx("button",{type:"button",className:`text-sm text-left ${o==="active"||o==="completed"?"font-medium":"text-muted-foreground"} ${o==="completed"?"hover:underline cursor-pointer":""}`,onClick:()=>{o==="completed"&&m(s)},disabled:o!=="completed",children:t.label}),o==="completed"&&S?e.jsx("div",{className:"text-sm text-muted-foreground mt-0.5",children:S}):o==="active"?e.jsx("div",{className:"text-xs text-muted-foreground italic mt-0.5",children:t.id==="setup"?"Not filled in":t.id==="budget"?"Not configured":"Not selected"}):null]})]},t.id)})})]})})]})})]})]})})})}},Xe=[{id:"setup",label:"Campaign setup"},{id:"budget",label:"Run time & budget"},{id:"review",label:"Media plan"}],q={render:()=>{const{theme:V}=fe(),G=ve(V||"retailMedia"),[u,m]=n.useState(0),[j,Z]=n.useState(""),[T,ee]=n.useState(""),[v,te]=n.useState([]),[L,ae]=n.useState(""),[ge,z]=n.useState(!1),[l,se]=n.useState(""),[r,re]=n.useState(void 0),[we,ke]=n.useState(!0),[H,K]=n.useState(()=>{const a={};return O.forEach(i=>{a[i.id]={mode:"preset",presetId:i.aiPreset.id}}),a}),c=X.find(a=>a.value===T),W=j.trim()!==""&&T!=="",p=l.trim()!==""&&r?.from!==void 0&&r?.to!==void 0;ue.filter(a=>a.name.toLowerCase().includes(L.toLowerCase())||a.id.includes(L)),n.useEffect(()=>{const a=i=>{i.target.closest("[data-dropdown-container]")||z(!1)};return document.addEventListener("mousedown",a),()=>document.removeEventListener("mousedown",a)},[]);const g=n.useMemo(()=>{let a=0,i=0,d=0,R=0;return Object.entries(H).forEach(([$,F])=>{if(!F||(R++,F.mode==="empty"))return;const f=O.find(ne=>ne.id===$);if(!f)return;const y=f.metrics.reach;y.endsWith("M")?a+=parseFloat(y.replace("M","")):y.endsWith("K")&&(a+=parseFloat(y.replace("K",""))/1e3);const U=parseFloat(f.metrics.roasChange.replace("%","").replace("+",""));i+=U;const oe=parseFloat(f.metrics.sales.replace("€","").replace(",",""));d+=oe}),{additionalReach:a,roasBoost:i,additionalSales:d,selectedCount:R}},[H]),E=a=>{switch(a){case"setup":return W?`${j} · ${c?.label}`:null;case"budget":return p?`€${l} total`:null;case"review":{const i=Object.values(H).filter(Boolean).length;return i===0?null:`${i} proposition${i!==1?"s":""}`}default:return null}},ie=(a,i)=>i<u?"completed":i===u?"active":"pending";return e.jsx(be,{children:e.jsx(pe,{routes:G,logo:{src:"/next.svg",alt:"Logo",width:40,height:40},user:{name:"Jane Doe",avatar:"https://ui-avatars.com/api/?name=Jane+Doe&size=32"},onLogout:()=>alert("Logout clicked"),breadcrumbProps:{namespace:""},pageHeaderProps:{title:"Create media experience",subtitle:"",headerRight:null},children:e.jsxs("div",{className:"space-y-6",children:[e.jsx(Ne,{metrics:[{key:"reach",label:"Est. Reach",value:g.selectedCount>0?`${g.additionalReach.toFixed(1)}M`:"-",subMetric:g.selectedCount>0?`${g.selectedCount} proposition${g.selectedCount!==1?"s":""}`:"No propositions selected",badgeValue:g.additionalReach>0?`${g.additionalReach.toFixed(1)}M`:void 0,badgeVariant:"success"},{key:"budget",label:"Budget",value:l.trim()!==""?`€${Number(l).toLocaleString()}`:"-",subMetric:l.trim()!==""?r?.from&&r?.to?`€${(parseFloat(l)/(Math.ceil((r.to.getTime()-r.from.getTime())/(1e3*60*60*24))+1)).toFixed(0)}/day`:"No dates set":"No budget set"},{key:"roas",label:"Est. ROAS",value:l.trim()!==""?`${((2.4+(parseFloat(l)>5e3?1.2:parseFloat(l)>2e3?.6:0))*(1+g.roasBoost/100)).toFixed(1)}x`:"-",subMetric:l.trim()!==""?"Predicted return":"Set budget to calculate",badgeValue:l.trim()!==""?g.roasBoost>0?`+${g.roasBoost}%`:"+12%":void 0,badgeVariant:"success"},{key:"sales",label:"Est. Sales",value:l.trim()!==""?(()=>{const a=2.4+(parseFloat(l)>5e3?1.2:parseFloat(l)>2e3?.6:0),d=parseFloat(l)*a+g.additionalSales;return`€${Math.round(d).toLocaleString()}`})():"-",subMetric:l.trim()!==""?"Projected revenue":"Set budget to calculate",badgeValue:l.trim()!==""?g.additionalSales>0?`+€${g.additionalSales.toLocaleString()}`:"Based on avg.":void 0,badgeVariant:g.additionalSales>0?"success":"secondary"}],selectedKeys:["reach","budget","roas","sales"],maxVisible:4,defaultVariant:"default",removable:!1}),e.jsxs("div",{className:"grid grid-cols-1 lg:grid-cols-3 gap-6",children:[e.jsxs("div",{className:"lg:col-span-2 min-w-0",children:[u===0&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsx(A,{className:"text-lg",children:"Campaign setup"}),e.jsx(I,{children:"Enter the basic details for your new media experience"})]}),e.jsxs(M,{children:[e.jsxs("div",{className:"space-y-6",children:[e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{htmlFor:"campaign-name-ng",children:"Campaign name"}),e.jsx(D,{id:"campaign-name-ng",placeholder:"e.g. Summer Sale 2026",value:j,onChange:a=>Z(a.target.value),hint:"Give your campaign a descriptive name to easily identify it later"})]}),e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{htmlFor:"brand-ng",children:"Brand"}),e.jsx(D,{dropdown:!0,options:X,value:T,onChange:a=>ee(a),placeholder:"Select a brand"}),e.jsx("div",{className:"text-xs text-muted-foreground mt-1",children:"Choose the brand this campaign will advertise for"})]})]}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",children:"Cancel"}),e.jsx(h,{disabled:!W,onClick:()=>m(1),children:"Continue"})]})]})]}),u===1&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsx(A,{className:"text-lg",children:"Run time & budget"}),e.jsx(I,{children:"Set when your campaign runs and how much you want to spend"})]}),e.jsxs(M,{children:[e.jsxs("div",{className:"space-y-6",children:[e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{children:"Run time"}),e.jsx(je,{dateRange:r,onDateRangeChange:re,placeholder:"Select start and end date",showPresets:!0}),e.jsx("div",{className:"text-xs text-muted-foreground",children:"Your campaign will automatically start and stop on the selected dates"})]}),e.jsxs("div",{className:"space-y-2",children:[e.jsx(w,{htmlFor:"budget-amount-ng",children:"Total budget"}),e.jsxs("div",{className:"relative",children:[e.jsx("span",{className:"absolute left-3 top-1/2 -translate-y-1/2 text-sm text-muted-foreground",children:"€"}),e.jsx(D,{id:"budget-amount-ng",type:"number",placeholder:"e.g. 5000",value:l,onChange:a=>se(a.target.value),className:"pl-7"})]}),e.jsx("div",{className:"text-xs text-muted-foreground",children:"The maximum total amount for the entire campaign duration"})]}),l&&r?.from&&r?.to&&e.jsxs("div",{className:"p-3 rounded-lg bg-primary/5 border border-primary/20",children:[e.jsxs("div",{className:"flex items-center justify-between",children:[e.jsx("span",{className:"text-sm font-medium",children:"Daily average"}),e.jsxs("span",{className:"text-sm font-semibold text-primary",children:["€",(parseFloat(l)/(Math.ceil((r.to.getTime()-r.from.getTime())/(1e3*60*60*24))+1)).toFixed(2),"/day"]})]}),e.jsxs("p",{className:"text-xs text-muted-foreground mt-1",children:["€",l," over ",Math.ceil((r.to.getTime()-r.from.getTime())/(1e3*60*60*24))+1," days"]})]})]}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",onClick:()=>m(0),children:"Back"}),e.jsx(h,{disabled:!p,onClick:()=>m(2),children:"Continue"})]})]})]}),u===2&&e.jsxs(B,{children:[e.jsxs(N,{children:[e.jsx(A,{className:"text-lg",children:"Media plan"}),e.jsx(I,{children:"Toggle propositions on or off and choose a preset campaign or start with an empty configuration."})]}),e.jsxs(M,{children:[e.jsx("div",{className:"space-y-4",children:O.map(a=>{const i=H[a.id],d=i!=null,R=i?.mode==="preset",$=i?.mode==="empty",F=a.icon;return e.jsxs("div",{className:b("rounded-lg border transition-all",d?"border-border":"border-border/50"),children:[e.jsxs("div",{className:"flex items-center gap-3 p-4",children:[e.jsx("div",{className:b("w-8 h-8 rounded-lg flex items-center justify-center transition-colors",d?"bg-primary text-primary-foreground":"bg-muted text-muted-foreground"),children:e.jsx(F,{size:16})}),e.jsxs("div",{className:"flex-1 min-w-0",children:[e.jsx("span",{className:b("text-sm font-semibold",!d&&"text-muted-foreground"),children:a.name}),e.jsx("p",{className:"text-xs text-muted-foreground",children:a.description})]}),e.jsxs("div",{className:"flex items-center gap-3 flex-shrink-0",children:[d&&e.jsx("span",{className:"text-xs text-muted-foreground",children:$?"– reach · – ROAS":`${a.metrics.reach} reach · ${a.metrics.roas} ROAS`}),e.jsx(le,{checked:d,onCheckedChange:f=>{K(f?y=>({...y,[a.id]:{mode:"preset",presetId:a.aiPreset.id}}):y=>({...y,[a.id]:null}))}})]})]}),d&&e.jsx("div",{className:"px-4 pb-4",children:e.jsxs("div",{className:"grid grid-cols-2 gap-3",children:[e.jsxs("button",{type:"button",onClick:()=>{K(f=>({...f,[a.id]:{mode:"preset",presetId:a.aiPreset.id}}))},className:b("flex flex-col items-start text-left p-4 rounded-lg border-2 transition-all cursor-pointer h-full",R?"border-primary bg-primary/5 shadow-sm":"border-border hover:border-primary/30"),children:[e.jsxs("div",{className:"flex items-center gap-2 mb-2",children:[e.jsx(me,{size:14,className:"text-primary flex-shrink-0"}),e.jsx("span",{className:"text-sm font-medium",children:"Automatic"}),e.jsx(Q,{variant:"secondary",className:"text-[10px] px-1.5 py-0",children:"AI"})]}),e.jsx("p",{className:"text-xs text-muted-foreground leading-relaxed mb-3",children:a.aiPreset.description}),e.jsxs("div",{className:"flex gap-3 mt-auto",children:[e.jsxs("span",{className:"text-xs text-muted-foreground",children:[a.aiPreset.placements," placements"]}),e.jsxs("span",{className:"text-xs text-muted-foreground",children:["~",a.aiPreset.estImpressions," imp."]})]})]}),e.jsxs("button",{type:"button",onClick:()=>{K(f=>({...f,[a.id]:{mode:"empty"}}))},className:b("flex flex-col items-start text-left p-4 rounded-lg border-2 border-dashed transition-all cursor-pointer h-full",$?"border-primary bg-primary/5 shadow-sm":"border-muted-foreground/20 hover:border-primary/30"),children:[e.jsxs("div",{className:"flex items-center gap-2 mb-2",children:[e.jsx(Re,{size:14,className:"text-muted-foreground flex-shrink-0"}),e.jsx("span",{className:"text-sm font-medium",children:"Empty campaign"})]}),e.jsxs("p",{className:"text-xs text-muted-foreground leading-relaxed",children:["Start with a blank ",a.name.toLowerCase()," campaign and configure manually"]})]})]})})]},a.id)})}),e.jsxs("div",{className:"flex justify-end gap-3 mt-8",children:[e.jsx(h,{variant:"ghost",onClick:()=>m(1),children:"Back"}),e.jsx(h,{onClick:()=>{const a=j||"New Media Experience";window.location.href=`/campaigns?new=${encodeURIComponent(a)}`},children:"Launch media experience"})]})]})]})]}),e.jsx("div",{className:"flex flex-col gap-4",children:e.jsxs(ye,{children:[e.jsx(N,{children:e.jsx(Se,{children:"Summary"})}),e.jsx(Ce,{children:e.jsxs("div",{className:"relative pl-12",children:[e.jsx("div",{className:"absolute left-[19px] top-[16px] bottom-[16px] w-px bg-border"}),e.jsx("div",{className:"space-y-4",children:Xe.map((a,i)=>{const d=ie(a.id,i),R=E(a.id);return e.jsxs("div",{className:"relative flex items-start -ml-12",children:[e.jsx("div",{className:"w-10 flex justify-center",children:e.jsx("div",{className:`w-8 h-8 rounded-full flex items-center justify-center text-xs font-medium ${d==="completed"?"bg-primary text-primary-foreground":d==="active"?"bg-background text-primary border-2 border-primary":"bg-background text-muted-foreground border border-border"}`,children:d==="completed"?e.jsx(ce,{size:14}):i+1})}),e.jsxs("div",{className:"ml-3 flex-1 min-w-0 pt-1",children:[e.jsx("button",{type:"button",className:`text-sm text-left ${d==="active"||d==="completed"?"font-medium":"text-muted-foreground"} ${d==="completed"?"hover:underline cursor-pointer":""}`,onClick:()=>{d==="completed"&&m(i)},disabled:d!=="completed",children:a.label}),d==="completed"&&R?e.jsx("div",{className:"text-sm text-muted-foreground mt-0.5",children:R}):d==="active"?e.jsx("div",{className:"text-xs text-muted-foreground italic mt-0.5",children:a.id==="setup"?"Not filled in":a.id==="budget"?"Not configured":"Not selected"}):null]})]},a.id)})})]})})]})})]})]})})})},parameters:{docs:{description:{story:`
# Create Media Experience – No Goal & Targeting

A streamlined variant of the Create Media Experience wizard that removes the Goal and Targeting steps. The wizard flows directly from Campaign Setup to Run Time & Budget, and then to Media Plan.

## Steps

1. **Campaign Setup** - Enter the campaign name, select brand, and choose retail products
2. **Run Time & Budget** - Set the campaign schedule and budget with auto-optimization
3. **Media Plan** - Toggle propositions and choose AI presets or empty campaigns

## Differences from Standard Wizard

- **No Campaign Goal step** (Step 2 removed)
- **No Targeting step** (Step 3 removed)
- **3-step wizard** instead of 5 steps
- Metric cards do not include audience-based reach calculations
- Summary sidebar shows only 3 steps

## Use Cases

- Platforms where goal and targeting are pre-configured or managed separately
- Simplified campaign creation workflows
- Quick campaign setup focused on budget and media selection
        `}}}};Y.parameters={...Y.parameters,docs:{...Y.parameters?.docs,source:{originalSource:`{
  render: () => {
    const {
      theme: storybookTheme
    } = useStorybookTheme();
    const currentTheme = storybookTheme || 'retailMedia';
    const routes = getRoutesForTheme(currentTheme);

    // Wizard state
    const [currentStep, setCurrentStep] = React.useState(0);

    // Step 1: Campaign Setup
    const [campaignName, setCampaignName] = React.useState('');
    const [selectedBrand, setSelectedBrand] = React.useState('');
    const [selectedRetailProducts, setSelectedRetailProducts] = React.useState<string[]>([]);
    const [retailProductSearch, setRetailProductSearch] = React.useState('');
    const [showRetailProductResults, setShowRetailProductResults] = React.useState(false);

    // Step 2: Campaign Goal
    const [selectedGoal, setSelectedGoal] = React.useState<string | null>(null);

    // Step 3: Targeting
    const [selectedAudiences, setSelectedAudiences] = React.useState<string[]>([]);
    const [tags, setTags] = React.useState<string[]>([]);
    const [tagInput, setTagInput] = React.useState('');

    // Step 4: Run time & Budget
    const [budgetAmount, setBudgetAmount] = React.useState('');
    const [dateRange, setDateRange] = React.useState<DateRange | undefined>(undefined);
    const [autoBudgetOptimization, setAutoBudgetOptimization] = React.useState(true);

    // Step 5: Media plan - proposition selections
    // Each key is a proposition id, value is 'preset' | 'empty' | null, plus selected preset id
    // Default: all propositions selected with AI preset
    const [propositionSelections, setPropositionSelections] = React.useState<Record<string, {
      mode: 'preset' | 'empty';
      presetId?: string;
    } | null>>(() => {
      const defaults: Record<string, {
        mode: 'preset' | 'empty';
        presetId?: string;
      }> = {};
      propositions.forEach(p => {
        defaults[p.id] = {
          mode: 'preset',
          presetId: p.aiPreset.id
        };
      });
      return defaults;
    });

    // Derived data
    const selectedGoalData = goals.find(g => g.id === selectedGoal);
    const selectedBrandData = brandOptions.find(b => b.value === selectedBrand);

    // Step completion checks
    const isSetupComplete = campaignName.trim() !== '' && selectedBrand !== '';
    const isGoalComplete = selectedGoal !== null;
    const isTargetingComplete = selectedAudiences.length > 0;
    const isBudgetComplete = budgetAmount.trim() !== '' && dateRange?.from !== undefined && dateRange?.to !== undefined;

    // Audience toggle
    const toggleAudience = (id: string) => {
      setSelectedAudiences(prev => prev.includes(id) ? prev.filter(a => a !== id) : [...prev, id]);
    };

    // Tag management
    const addTag = (tag: string) => {
      const trimmed = tag.trim();
      if (trimmed && !tags.includes(trimmed)) {
        setTags(prev => [...prev, trimmed]);
      }
      setTagInput('');
    };
    const removeTag = (tag: string) => {
      setTags(prev => prev.filter(t => t !== tag));
    };
    const handleTagKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
      if (e.key === 'Enter' || e.key === ',') {
        e.preventDefault();
        addTag(tagInput);
      }
    };

    // Retail product helpers
    const filteredRetailProducts = retailProducts.filter(product => product.name.toLowerCase().includes(retailProductSearch.toLowerCase()) || product.id.includes(retailProductSearch));
    const handleRetailProductSelect = (product: {
      id: string;
      name: string;
    }) => {
      if (!selectedRetailProducts.includes(product.id)) {
        setSelectedRetailProducts([...selectedRetailProducts, product.id]);
      }
      setRetailProductSearch('');
      setShowRetailProductResults(false);
    };
    const handleRetailProductSearchChange = (e: React.ChangeEvent<HTMLInputElement>) => {
      setRetailProductSearch(e.target.value);
      setShowRetailProductResults(e.target.value.length > 0);
    };
    const removeRetailProduct = (productId: string) => {
      setSelectedRetailProducts(selectedRetailProducts.filter(id => id !== productId));
    };

    // Close dropdown when clicking outside
    React.useEffect(() => {
      const handleClickOutside = (event: MouseEvent) => {
        const target = event.target as HTMLElement;
        if (!target.closest('[data-dropdown-container]')) {
          setShowRetailProductResults(false);
        }
      };
      document.addEventListener('mousedown', handleClickOutside);
      return () => document.removeEventListener('mousedown', handleClickOutside);
    }, []);

    // Get step value for summary
    const getStepValue = (stepId: string): string | null => {
      switch (stepId) {
        case 'setup':
          if (isSetupComplete) return \`\${campaignName} · \${selectedBrandData?.label}\`;
          return null;
        case 'goal':
          if (selectedGoalData) return selectedGoalData.title;
          return null;
        case 'targeting':
          {
            if (!isTargetingComplete) return null;
            const audienceCount = selectedAudiences.length;
            return \`\${audienceCount} audience\${audienceCount !== 1 ? 's' : ''}\`;
          }
        case 'budget':
          {
            if (!isBudgetComplete) return null;
            return \`€\${budgetAmount} total\`;
          }
        case 'review':
          {
            const selectedCount = Object.values(propositionSelections).filter(Boolean).length;
            if (selectedCount === 0) return null;
            return \`\${selectedCount} proposition\${selectedCount !== 1 ? 's' : ''}\`;
          }
        default:
          return null;
      }
    };

    // Get step status
    const getStepStatus = (stepId: string, stepIndex: number): 'completed' | 'active' | 'pending' => {
      if (stepIndex < currentStep) return 'completed';
      if (stepIndex === currentStep) return 'active';
      return 'pending';
    };

    // Estimated reach calculation
    const estimatedReach = React.useMemo(() => {
      if (selectedAudiences.length === 0) return null;
      let total = 0;
      selectedAudiences.forEach(id => {
        const audience = audienceOptions.find(a => a.id === id);
        if (audience) {
          const num = parseFloat(audience.reach.replace('M', ''));
          total += num;
        }
      });
      // Cap at a realistic number (overlapping audiences)
      const adjusted = Math.min(total * 0.8, 15);
      return \`\${adjusted.toFixed(1)}M\`;
    }, [selectedAudiences]);

    // Proposition impact on metrics
    const propositionImpact = React.useMemo(() => {
      let additionalReach = 0;
      let roasBoost = 0;
      let additionalSales = 0;
      let selectedCount = 0;
      Object.entries(propositionSelections).forEach(([propId, sel]) => {
        if (!sel) return;
        selectedCount++;
        // Empty campaigns have unknown metrics, so skip them
        if (sel.mode === 'empty') return;
        const prop = propositions.find(p => p.id === propId);
        if (!prop) return;
        // Parse reach (e.g. '3.2M' or '680K')
        const reachStr = prop.metrics.reach;
        if (reachStr.endsWith('M')) {
          additionalReach += parseFloat(reachStr.replace('M', ''));
        } else if (reachStr.endsWith('K')) {
          additionalReach += parseFloat(reachStr.replace('K', '')) / 1000;
        }
        // Parse ROAS change (e.g. '+8%')
        const roasChangeNum = parseFloat(prop.metrics.roasChange.replace('%', '').replace('+', ''));
        roasBoost += roasChangeNum;
        // Parse sales (e.g. '€8,400')
        const salesNum = parseFloat(prop.metrics.sales.replace('€', '').replace(',', ''));
        additionalSales += salesNum;
      });
      return {
        additionalReach,
        roasBoost,
        additionalSales,
        selectedCount
      };
    }, [propositionSelections]);
    return <MenuContextProvider>
        <AppLayout routes={routes} logo={{
        src: '/next.svg',
        alt: 'Logo',
        width: 40,
        height: 40
      }} user={{
        name: 'Jane Doe',
        avatar: 'https://ui-avatars.com/api/?name=Jane+Doe&size=32'
      }} onLogout={() => alert('Logout clicked')} breadcrumbProps={{
        namespace: ''
      }} pageHeaderProps={{
        title: 'Create media experience',
        subtitle: '',
        headerRight: null
      }}>
          <div className="space-y-6">
            {/* Metric cards - always visible, show '-' when no data */}
              <MetricRow metrics={[{
            key: 'reach',
            label: 'Est. Reach',
            value: selectedAudiences.length > 0 ? (() => {
              const baseReach = parseFloat(estimatedReach?.replace('M', '') || '0');
              const totalReach = baseReach + propositionImpact.additionalReach;
              return \`\${totalReach.toFixed(1)}M\`;
            })() : '-',
            subMetric: selectedAudiences.length > 0 ? propositionImpact.selectedCount > 0 ? \`\${selectedAudiences.length} audience\${selectedAudiences.length !== 1 ? 's' : ''} · \${propositionImpact.selectedCount} proposition\${propositionImpact.selectedCount !== 1 ? 's' : ''}\` : \`\${selectedAudiences.length} audience\${selectedAudiences.length !== 1 ? 's' : ''}\` : 'No audience selected',
            badgeValue: selectedAudiences.length > 0 && propositionImpact.additionalReach > 0 ? \`+\${propositionImpact.additionalReach.toFixed(1)}M\` : selectedAudiences.length > 1 ? 'Combined' : undefined,
            badgeVariant: propositionImpact.additionalReach > 0 ? 'success' as const : 'info' as const
          }, {
            key: 'budget',
            label: 'Budget',
            value: budgetAmount.trim() !== '' ? \`€\${Number(budgetAmount).toLocaleString()}\` : '-',
            subMetric: budgetAmount.trim() !== '' ? dateRange?.from && dateRange?.to ? \`€\${(parseFloat(budgetAmount) / (Math.ceil((dateRange.to.getTime() - dateRange.from.getTime()) / (1000 * 60 * 60 * 24)) + 1)).toFixed(0)}/day\` : 'No dates set' : 'No budget set'
          }, {
            key: 'roas',
            label: 'Est. ROAS',
            value: budgetAmount.trim() !== '' ? (() => {
              const baseRoas = 2.4 + (parseFloat(budgetAmount) > 5000 ? 1.2 : parseFloat(budgetAmount) > 2000 ? 0.6 : 0) + (selectedAudiences.length > 2 ? 0.5 : 0);
              const boostedRoas = baseRoas * (1 + propositionImpact.roasBoost / 100);
              return \`\${boostedRoas.toFixed(1)}x\`;
            })() : '-',
            subMetric: budgetAmount.trim() !== '' ? 'Predicted return' : 'Set budget to calculate',
            badgeValue: budgetAmount.trim() !== '' ? propositionImpact.roasBoost > 0 ? \`+\${propositionImpact.roasBoost}%\` : '+12%' : undefined,
            badgeVariant: 'success' as const
          }, {
            key: 'sales',
            label: 'Est. Sales',
            value: budgetAmount.trim() !== '' ? (() => {
              const baseRoas = 2.4 + (parseFloat(budgetAmount) > 5000 ? 1.2 : parseFloat(budgetAmount) > 2000 ? 0.6 : 0) + (selectedAudiences.length > 2 ? 0.5 : 0);
              const baseSales = parseFloat(budgetAmount) * baseRoas;
              const totalSales = baseSales + propositionImpact.additionalSales;
              return \`€\${Math.round(totalSales).toLocaleString()}\`;
            })() : '-',
            subMetric: budgetAmount.trim() !== '' ? 'Projected revenue' : 'Set budget to calculate',
            badgeValue: budgetAmount.trim() !== '' ? propositionImpact.additionalSales > 0 ? \`+€\${propositionImpact.additionalSales.toLocaleString()}\` : 'Based on avg.' : undefined,
            badgeVariant: propositionImpact.additionalSales > 0 ? 'success' as const : 'secondary' as const
          }]} selectedKeys={['reach', 'budget', 'roas', 'sales']} maxVisible={4} defaultVariant="default" removable={false} />

            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
            {/* Main content */}
            <div className="lg:col-span-2 min-w-0">

              {/* Step 1: Campaign Setup */}
              {currentStep === 0 && <Card>
                  <CardHeader>
                    <CardTitle className="text-lg">Campaign setup</CardTitle>
                    <CardDescription>
                      Enter the basic details for your new media experience
                    </CardDescription>
                  </CardHeader>
                  <CardContent>
                    <div className="space-y-6">
                      <div className="space-y-2">
                        <Label htmlFor="campaign-name">Campaign name</Label>
                        <Input id="campaign-name" placeholder="e.g. Summer Sale 2026" value={campaignName} onChange={(e: React.ChangeEvent<HTMLInputElement>) => setCampaignName(e.target.value)} hint="Give your campaign a descriptive name to easily identify it later" />
                      </div>
                      <div className="space-y-2">
                        <Label htmlFor="brand">Brand</Label>
                        <Input dropdown options={brandOptions} value={selectedBrand} onChange={(value: string) => setSelectedBrand(value)} placeholder="Select a brand" />
                        <div className="text-xs text-muted-foreground mt-1">Choose the brand this campaign will advertise for</div>
                      </div>
                      <div className="space-y-2">
                        <Label>Retail products</Label>
                        <div className="relative" data-dropdown-container>
                          <SearchInput value={retailProductSearch} onChange={handleRetailProductSearchChange} onClick={() => setShowRetailProductResults(true)} placeholder="Select product by name or ID..." className="w-full" icon={<ScanBarcode className="w-4 h-4" />} />
                          {showRetailProductResults && <div className="absolute z-10 w-full mt-1 bg-background border rounded-md shadow-lg max-h-60 overflow-y-auto">
                              {filteredRetailProducts.length > 0 ? filteredRetailProducts.map(product => <div key={product.id} className={\`p-3 hover:bg-muted/50 cursor-pointer border-b last:border-b-0 \${selectedRetailProducts.includes(product.id) ? 'bg-primary/5' : ''}\`} onClick={() => handleRetailProductSelect(product)}>
                                    <div className="flex items-center justify-between">
                                      <span className="text-sm">{product.name}</span>
                                      <span className="text-xs text-muted-foreground">#{product.id}</span>
                                    </div>
                                  </div>) : <div className="p-3 text-sm text-muted-foreground">No products found</div>}
                            </div>}
                        </div>
                        {selectedRetailProducts.length > 0 && <div className="flex flex-wrap gap-2 mt-2">
                            {selectedRetailProducts.map(productId => {
                          const product = retailProducts.find(p => p.id === productId);
                          if (!product) return null;
                          return <Badge key={productId} variant="default" size="large" className="gap-1 pr-1.5">
                                  {product.name}
                                  <button type="button" onClick={() => removeRetailProduct(productId)} className="ml-1 hover:text-destructive transition-colors rounded-full">
                                    <X size={14} />
                                  </button>
                                </Badge>;
                        })}
                          </div>}
                        <div className="text-xs text-muted-foreground">
                          {selectedRetailProducts.length > 0 ? \`\${selectedRetailProducts.length} retail product\${selectedRetailProducts.length > 1 ? 's' : ''} selected\` : 'Search and select retail products to target for this campaign'}
                        </div>
                      </div>
                    </div>
                    <div className="flex justify-end gap-3 mt-8">
                      <Button variant="ghost">Cancel</Button>
                      <Button disabled={!isSetupComplete} onClick={() => setCurrentStep(1)}>
                        Continue
                      </Button>
                    </div>
                  </CardContent>
                </Card>}

              {/* Step 2: Campaign Goal */}
              {currentStep === 1 && <Card>
                  <CardHeader>
                    <CardTitle className="text-lg">What is your campaign goal?</CardTitle>
                    <CardDescription>
                      Select a goal to align the objectives and settings that work best for your campaign
                    </CardDescription>
                  </CardHeader>
                  <CardContent>
                    <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                      {goals.map(goal => <GoalCard key={goal.id} icon={goal.icon} title={goal.title} description={goal.description} selected={selectedGoal === goal.id} onClick={() => setSelectedGoal(goal.id)} />)}
                    </div>
                    <div className="flex justify-end gap-3 mt-8">
                      <Button variant="ghost" onClick={() => setCurrentStep(0)}>Back</Button>
                      <Button disabled={!selectedGoal} onClick={() => setCurrentStep(2)}>
                        Continue
                      </Button>
                    </div>
                  </CardContent>
                </Card>}

              {/* Step 3: Targeting */}
              {currentStep === 2 && <Card>
                    <CardHeader>
                      <CardTitle className="text-lg flex items-center gap-2">
                        <Users size={20} />
                        Select your audience
                      </CardTitle>
                      <CardDescription>
                        Choose one or more audience segments to target with your campaign
                      </CardDescription>
                    </CardHeader>
                    <CardContent>
                      <div className="space-y-3">
                        {audienceOptions.map(audience => {
                      const isSelected = selectedAudiences.includes(audience.id);
                      return <button key={audience.id} type="button" onClick={() => toggleAudience(audience.id)} className={\`w-full flex items-start gap-3 p-4 rounded-lg border transition-all text-left \${isSelected ? 'border-primary bg-primary/5' : 'border-border hover:border-primary/30'}\`}>
                              <div className={cn("mt-0.5 h-4 w-4 shrink-0 rounded-sm border border-primary flex items-center justify-center", isSelected && "bg-primary text-primary-foreground")}>
                                {isSelected && <Check className="h-4 w-4" />}
                              </div>
                              <div className="flex-1 min-w-0">
                                <div className="flex items-center justify-between gap-2">
                                  <span className="text-sm font-medium">{audience.label}</span>
                                  <span className="text-xs text-muted-foreground flex-shrink-0">Reach: {audience.reach}</span>
                                </div>
                                <p className="text-xs text-muted-foreground mt-0.5">{audience.description}</p>
                              </div>
                            </button>;
                    })}
                      </div>
                      {estimatedReach && <div className="mt-4 p-3 rounded-lg bg-primary/5 border border-primary/20">
                          <div className="flex items-center justify-between">
                            <span className="text-sm font-medium">Estimated reach</span>
                            <span className="text-sm font-semibold text-primary">{estimatedReach} shoppers</span>
                          </div>
                        </div>}
                      <div className="flex justify-end gap-3 mt-8">
                        <Button variant="ghost" onClick={() => setCurrentStep(1)}>Back</Button>
                        <Button disabled={!isTargetingComplete} onClick={() => setCurrentStep(3)}>
                          Continue
                        </Button>
                      </div>
                    </CardContent>
                  </Card>}

              {/* Step 4: Run time & Budget */}
              {currentStep === 3 && <Card>
                  <CardHeader>
                    <CardTitle className="text-lg">Run time & budget</CardTitle>
                    <CardDescription>
                      Set when your campaign runs and how much you want to spend
                    </CardDescription>
                  </CardHeader>
                  <CardContent>
                    <div className="space-y-6">
                      <div className="space-y-2">
                        <Label>Run time</Label>
                        <DateRangePicker dateRange={dateRange} onDateRangeChange={setDateRange} placeholder="Select start and end date" showPresets />
                        <div className="text-xs text-muted-foreground">
                          Your campaign will automatically start and stop on the selected dates
                        </div>
                      </div>

                      <div className="space-y-2">
                        <Label htmlFor="budget-amount">Total budget</Label>
                        <div className="relative">
                          <span className="absolute left-3 top-1/2 -translate-y-1/2 text-sm text-muted-foreground">€</span>
                          <Input id="budget-amount" type="number" placeholder="e.g. 5000" value={budgetAmount} onChange={(e: React.ChangeEvent<HTMLInputElement>) => setBudgetAmount(e.target.value)} className="pl-7" />
                        </div>
                        <div className="text-xs text-muted-foreground">
                          The maximum total amount for the entire campaign duration
                        </div>
                      </div>

                      {budgetAmount && dateRange?.from && dateRange?.to && <div className="p-3 rounded-lg bg-primary/5 border border-primary/20">
                          <div className="flex items-center justify-between">
                            <span className="text-sm font-medium">Daily average</span>
                            <span className="text-sm font-semibold text-primary">
                              €{(parseFloat(budgetAmount) / (Math.ceil((dateRange.to.getTime() - dateRange.from.getTime()) / (1000 * 60 * 60 * 24)) + 1)).toFixed(2)}/day
                            </span>
                          </div>
                          <p className="text-xs text-muted-foreground mt-1">
                            €{budgetAmount} over {Math.ceil((dateRange.to.getTime() - dateRange.from.getTime()) / (1000 * 60 * 60 * 24)) + 1} days
                          </p>
                        </div>}

                      {/* Auto Budget Optimization */}
                      <div className={cn("rounded-lg border p-4 transition-all", autoBudgetOptimization ? 'border-primary/30 bg-primary/5' : 'border-border')}>
                        <div className="flex items-start gap-3">
                          <div className="flex-1 min-w-0">
                            <div className="flex items-center gap-2">
                              <Sparkles size={14} className={autoBudgetOptimization ? 'text-primary' : 'text-muted-foreground'} />
                              <span className="text-sm font-medium">Auto budget optimization</span>
                              {autoBudgetOptimization && <Badge variant="secondary" className="text-[10px] px-1.5 py-0">AI</Badge>}
                            </div>
                            <p className="text-xs text-muted-foreground mt-1">
                              Automatically distribute your budget across propositions based on real-time performance data to maximise ROAS
                            </p>
                          </div>
                          <Switch checked={autoBudgetOptimization} onCheckedChange={setAutoBudgetOptimization} />
                        </div>
                        {autoBudgetOptimization && budgetAmount && <div className="mt-3 pt-3 border-t border-primary/20 flex gap-4">
                            <div className="flex items-center gap-1.5">
                              <TrendingUp size={12} className="text-primary" />
                              <span className="text-xs text-muted-foreground">Avg. +18% ROAS improvement</span>
                            </div>
                            <div className="flex items-center gap-1.5">
                              <DollarSign size={12} className="text-primary" />
                              <span className="text-xs text-muted-foreground">Real-time rebalancing</span>
                            </div>
                          </div>}
                      </div>
                    </div>

                    <div className="flex justify-end gap-3 mt-8">
                      <Button variant="ghost" onClick={() => setCurrentStep(2)}>Back</Button>
                      <Button disabled={!isBudgetComplete} onClick={() => setCurrentStep(4)}>
                        Continue
                      </Button>
                    </div>
                  </CardContent>
                </Card>}

              {/* Step 5: Media plan */}
              {currentStep === 4 && <Card>
                  <CardHeader>
                    <CardTitle className="text-lg">Media plan</CardTitle>
                    <CardDescription>
                      Toggle propositions on or off and choose a preset campaign or start with an empty configuration.
                    </CardDescription>
                  </CardHeader>
                  <CardContent>
                    <div className="space-y-4">
                      {propositions.map(prop => {
                      const selection = propositionSelections[prop.id];
                      const isEnabled = selection !== null && selection !== undefined;
                      const isAiSelected = selection?.mode === 'preset';
                      const isEmptySelected = selection?.mode === 'empty';
                      const IconComponent = prop.icon;
                      return <div key={prop.id} className={cn("rounded-lg border transition-all", isEnabled ? 'border-border' : 'border-border/50')}>
                            {/* Proposition header with toggle */}
                            <div className="flex items-center gap-3 p-4">
                              <div className={cn("w-8 h-8 rounded-lg flex items-center justify-center transition-colors", isEnabled ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground')}>
                                <IconComponent size={16} />
                              </div>
                              <div className="flex-1 min-w-0">
                                <span className={cn("text-sm font-semibold", !isEnabled && 'text-muted-foreground')}>{prop.name}</span>
                                <p className="text-xs text-muted-foreground">{prop.description}</p>
                              </div>
                              <div className="flex items-center gap-3 flex-shrink-0">
                                {isEnabled && <span className="text-xs text-muted-foreground">
                                    {isEmptySelected ? '– reach · – ROAS' : \`\${prop.metrics.reach} reach · \${prop.metrics.roas} ROAS\`}
                                  </span>}
                                <Switch checked={isEnabled} onCheckedChange={(checked: boolean) => {
                              if (checked) {
                                setPropositionSelections(prev => ({
                                  ...prev,
                                  [prop.id]: {
                                    mode: 'preset',
                                    presetId: prop.aiPreset.id
                                  }
                                }));
                              } else {
                                setPropositionSelections(prev => ({
                                  ...prev,
                                  [prop.id]: null
                                }));
                              }
                            }} />
                              </div>
                            </div>

                            {/* Expanded content when enabled */}
                            {isEnabled && <div className="px-4 pb-4">
                                <div className="grid grid-cols-2 gap-3">
                                  {/* AI preset option */}
                                  <button type="button" onClick={() => {
                              setPropositionSelections(prev => ({
                                ...prev,
                                [prop.id]: {
                                  mode: 'preset',
                                  presetId: prop.aiPreset.id
                                }
                              }));
                            }} className={cn("flex flex-col items-start text-left p-4 rounded-lg border-2 transition-all cursor-pointer h-full", isAiSelected ? 'border-primary bg-primary/5 shadow-sm' : 'border-border hover:border-primary/30')}>
                                    <div className="flex items-center gap-2 mb-2">
                                      <Sparkles size={14} className="text-primary flex-shrink-0" />
                                      <span className="text-sm font-medium">Automatic</span>
                                      <Badge variant="secondary" className="text-[10px] px-1.5 py-0">AI</Badge>
                                    </div>
                                    <p className="text-xs text-muted-foreground leading-relaxed mb-3">{prop.aiPreset.description}</p>
                                    <div className="flex gap-3 mt-auto">
                                      <span className="text-xs text-muted-foreground">{prop.aiPreset.placements} placements</span>
                                      <span className="text-xs text-muted-foreground">~{prop.aiPreset.estImpressions} imp.</span>
                                    </div>
                                  </button>

                                  {/* Empty campaign option */}
                                  <button type="button" onClick={() => {
                              setPropositionSelections(prev => ({
                                ...prev,
                                [prop.id]: {
                                  mode: 'empty'
                                }
                              }));
                            }} className={cn("flex flex-col items-start text-left p-4 rounded-lg border-2 border-dashed transition-all cursor-pointer h-full", isEmptySelected ? 'border-primary bg-primary/5 shadow-sm' : 'border-muted-foreground/20 hover:border-primary/30')}>
                                    <div className="flex items-center gap-2 mb-2">
                                      <FileText size={14} className="text-muted-foreground flex-shrink-0" />
                                      <span className="text-sm font-medium">Empty campaign</span>
                                    </div>
                                    <p className="text-xs text-muted-foreground leading-relaxed">
                                      Start with a blank {prop.name.toLowerCase()} campaign and configure manually
                                    </p>
                                  </button>
                                </div>
                              </div>}
                          </div>;
                    })}
                    </div>
                    <div className="flex justify-end gap-3 mt-8">
                      <Button variant="ghost" onClick={() => setCurrentStep(3)}>Back</Button>
                      <Button onClick={() => {
                      // Navigate to media experiences overview with new campaign expanded
                      const name = campaignName || 'New Media Experience';
                      window.location.href = \`/campaigns?new=\${encodeURIComponent(name)}\`;
                    }}>
                        Launch media experience
                      </Button>
                    </div>
                  </CardContent>
                </Card>}

            </div>

            {/* Summary sidebar */}
            <div className="flex flex-col gap-4">
              <CardSummary>
                <CardHeader>
                  <CardSummaryTitle>Summary</CardSummaryTitle>
                </CardHeader>
                <CardSummaryContent>
                  <div className="relative pl-12">
                    {/* Vertical timeline line */}
                    <div className="absolute left-[19px] top-[16px] bottom-[16px] w-px bg-border"></div>

                    <div className="space-y-4">
                      {wizardSteps.map((step, index) => {
                        const status = getStepStatus(step.id, index);
                        const stepValue = getStepValue(step.id);
                        return <div key={step.id} className="relative flex items-start -ml-12">
                            {/* Circle on the timeline */}
                            <div className="w-10 flex justify-center">
                              <div className={\`w-8 h-8 rounded-full flex items-center justify-center text-xs font-medium \${status === 'completed' ? 'bg-primary text-primary-foreground' : status === 'active' ? 'bg-background text-primary border-2 border-primary' : 'bg-background text-muted-foreground border border-border'}\`}>
                                {status === 'completed' ? <Check size={14} /> : index + 1}
                              </div>
                            </div>
                            {/* Step content */}
                            <div className="ml-3 flex-1 min-w-0 pt-1">
                              <button type="button" className={\`text-sm text-left \${status === 'active' || status === 'completed' ? 'font-medium' : 'text-muted-foreground'} \${status === 'completed' ? 'hover:underline cursor-pointer' : ''}\`} onClick={() => {
                              if (status === 'completed') setCurrentStep(index);
                            }} disabled={status !== 'completed'}>
                                {step.label}
                              </button>
                              {status === 'completed' && stepValue ? <div className="text-sm text-muted-foreground mt-0.5">{stepValue}</div> : status === 'active' ? <div className="text-xs text-muted-foreground italic mt-0.5">
                                  {step.id === 'setup' ? 'Not filled in' : step.id === 'budget' ? 'Not configured' : 'Not selected'}
                                </div> : null}
                            </div>
                          </div>;
                      })}
                    </div>
                  </div>
                </CardSummaryContent>
              </CardSummary>
            </div>
          </div>
          </div>
        </AppLayout>
      </MenuContextProvider>;
  }
}`,...Y.parameters?.docs?.source}}};q.parameters={...q.parameters,docs:{...q.parameters?.docs,source:{originalSource:`{
  render: () => {
    const {
      theme: storybookTheme
    } = useStorybookTheme();
    const currentTheme = storybookTheme || 'retailMedia';
    const routes = getRoutesForTheme(currentTheme);

    // Wizard state
    const [currentStep, setCurrentStep] = React.useState(0);

    // Step 1: Campaign Setup
    const [campaignName, setCampaignName] = React.useState('');
    const [selectedBrand, setSelectedBrand] = React.useState('');
    const [selectedRetailProducts, setSelectedRetailProducts] = React.useState<string[]>([]);
    const [retailProductSearch, setRetailProductSearch] = React.useState('');
    const [showRetailProductResults, setShowRetailProductResults] = React.useState(false);

    // Step 2: Run time & Budget (was step 4)
    const [budgetAmount, setBudgetAmount] = React.useState('');
    const [dateRange, setDateRange] = React.useState<DateRange | undefined>(undefined);
    const [autoBudgetOptimization, setAutoBudgetOptimization] = React.useState(true);

    // Step 3: Media plan (was step 5)
    const [propositionSelections, setPropositionSelections] = React.useState<Record<string, {
      mode: 'preset' | 'empty';
      presetId?: string;
    } | null>>(() => {
      const defaults: Record<string, {
        mode: 'preset' | 'empty';
        presetId?: string;
      }> = {};
      propositions.forEach(p => {
        defaults[p.id] = {
          mode: 'preset',
          presetId: p.aiPreset.id
        };
      });
      return defaults;
    });

    // Derived data
    const selectedBrandData = brandOptions.find(b => b.value === selectedBrand);

    // Step completion checks
    const isSetupComplete = campaignName.trim() !== '' && selectedBrand !== '';
    const isBudgetComplete = budgetAmount.trim() !== '' && dateRange?.from !== undefined && dateRange?.to !== undefined;

    // Retail product helpers
    const filteredRetailProducts = retailProducts.filter(product => product.name.toLowerCase().includes(retailProductSearch.toLowerCase()) || product.id.includes(retailProductSearch));
    const handleRetailProductSelect = (product: {
      id: string;
      name: string;
    }) => {
      if (!selectedRetailProducts.includes(product.id)) {
        setSelectedRetailProducts([...selectedRetailProducts, product.id]);
      }
      setRetailProductSearch('');
      setShowRetailProductResults(false);
    };
    const handleRetailProductSearchChange = (e: React.ChangeEvent<HTMLInputElement>) => {
      setRetailProductSearch(e.target.value);
      setShowRetailProductResults(e.target.value.length > 0);
    };
    const removeRetailProduct = (productId: string) => {
      setSelectedRetailProducts(selectedRetailProducts.filter(id => id !== productId));
    };

    // Close dropdown when clicking outside
    React.useEffect(() => {
      const handleClickOutside = (event: MouseEvent) => {
        const target = event.target as HTMLElement;
        if (!target.closest('[data-dropdown-container]')) {
          setShowRetailProductResults(false);
        }
      };
      document.addEventListener('mousedown', handleClickOutside);
      return () => document.removeEventListener('mousedown', handleClickOutside);
    }, []);

    // Proposition impact on metrics
    const propositionImpact = React.useMemo(() => {
      let additionalReach = 0;
      let roasBoost = 0;
      let additionalSales = 0;
      let selectedCount = 0;
      Object.entries(propositionSelections).forEach(([propId, sel]) => {
        if (!sel) return;
        selectedCount++;
        if (sel.mode === 'empty') return;
        const prop = propositions.find(p => p.id === propId);
        if (!prop) return;
        const reachStr = prop.metrics.reach;
        if (reachStr.endsWith('M')) {
          additionalReach += parseFloat(reachStr.replace('M', ''));
        } else if (reachStr.endsWith('K')) {
          additionalReach += parseFloat(reachStr.replace('K', '')) / 1000;
        }
        const roasChangeNum = parseFloat(prop.metrics.roasChange.replace('%', '').replace('+', ''));
        roasBoost += roasChangeNum;
        const salesNum = parseFloat(prop.metrics.sales.replace('€', '').replace(',', ''));
        additionalSales += salesNum;
      });
      return {
        additionalReach,
        roasBoost,
        additionalSales,
        selectedCount
      };
    }, [propositionSelections]);

    // Get step value for summary
    const getStepValue = (stepId: string): string | null => {
      switch (stepId) {
        case 'setup':
          if (isSetupComplete) return \`\${campaignName} · \${selectedBrandData?.label}\`;
          return null;
        case 'budget':
          {
            if (!isBudgetComplete) return null;
            return \`€\${budgetAmount} total\`;
          }
        case 'review':
          {
            const selectedCount = Object.values(propositionSelections).filter(Boolean).length;
            if (selectedCount === 0) return null;
            return \`\${selectedCount} proposition\${selectedCount !== 1 ? 's' : ''}\`;
          }
        default:
          return null;
      }
    };

    // Get step status
    const getStepStatus = (stepId: string, stepIndex: number): 'completed' | 'active' | 'pending' => {
      if (stepIndex < currentStep) return 'completed';
      if (stepIndex === currentStep) return 'active';
      return 'pending';
    };
    return <MenuContextProvider>
        <AppLayout routes={routes} logo={{
        src: '/next.svg',
        alt: 'Logo',
        width: 40,
        height: 40
      }} user={{
        name: 'Jane Doe',
        avatar: 'https://ui-avatars.com/api/?name=Jane+Doe&size=32'
      }} onLogout={() => alert('Logout clicked')} breadcrumbProps={{
        namespace: ''
      }} pageHeaderProps={{
        title: 'Create media experience',
        subtitle: '',
        headerRight: null
      }}>
          <div className="space-y-6">
            {/* Metric cards */}
              <MetricRow metrics={[{
            key: 'reach',
            label: 'Est. Reach',
            value: propositionImpact.selectedCount > 0 ? \`\${propositionImpact.additionalReach.toFixed(1)}M\` : '-',
            subMetric: propositionImpact.selectedCount > 0 ? \`\${propositionImpact.selectedCount} proposition\${propositionImpact.selectedCount !== 1 ? 's' : ''}\` : 'No propositions selected',
            badgeValue: propositionImpact.additionalReach > 0 ? \`\${propositionImpact.additionalReach.toFixed(1)}M\` : undefined,
            badgeVariant: 'success' as const
          }, {
            key: 'budget',
            label: 'Budget',
            value: budgetAmount.trim() !== '' ? \`€\${Number(budgetAmount).toLocaleString()}\` : '-',
            subMetric: budgetAmount.trim() !== '' ? dateRange?.from && dateRange?.to ? \`€\${(parseFloat(budgetAmount) / (Math.ceil((dateRange.to.getTime() - dateRange.from.getTime()) / (1000 * 60 * 60 * 24)) + 1)).toFixed(0)}/day\` : 'No dates set' : 'No budget set'
          }, {
            key: 'roas',
            label: 'Est. ROAS',
            value: budgetAmount.trim() !== '' ? (() => {
              const baseRoas = 2.4 + (parseFloat(budgetAmount) > 5000 ? 1.2 : parseFloat(budgetAmount) > 2000 ? 0.6 : 0);
              const boostedRoas = baseRoas * (1 + propositionImpact.roasBoost / 100);
              return \`\${boostedRoas.toFixed(1)}x\`;
            })() : '-',
            subMetric: budgetAmount.trim() !== '' ? 'Predicted return' : 'Set budget to calculate',
            badgeValue: budgetAmount.trim() !== '' ? propositionImpact.roasBoost > 0 ? \`+\${propositionImpact.roasBoost}%\` : '+12%' : undefined,
            badgeVariant: 'success' as const
          }, {
            key: 'sales',
            label: 'Est. Sales',
            value: budgetAmount.trim() !== '' ? (() => {
              const baseRoas = 2.4 + (parseFloat(budgetAmount) > 5000 ? 1.2 : parseFloat(budgetAmount) > 2000 ? 0.6 : 0);
              const baseSales = parseFloat(budgetAmount) * baseRoas;
              const totalSales = baseSales + propositionImpact.additionalSales;
              return \`€\${Math.round(totalSales).toLocaleString()}\`;
            })() : '-',
            subMetric: budgetAmount.trim() !== '' ? 'Projected revenue' : 'Set budget to calculate',
            badgeValue: budgetAmount.trim() !== '' ? propositionImpact.additionalSales > 0 ? \`+€\${propositionImpact.additionalSales.toLocaleString()}\` : 'Based on avg.' : undefined,
            badgeVariant: propositionImpact.additionalSales > 0 ? 'success' as const : 'secondary' as const
          }]} selectedKeys={['reach', 'budget', 'roas', 'sales']} maxVisible={4} defaultVariant="default" removable={false} />

            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
            {/* Main content */}
            <div className="lg:col-span-2 min-w-0">

              {/* Step 1: Campaign Setup */}
              {currentStep === 0 && <Card>
                  <CardHeader>
                    <CardTitle className="text-lg">Campaign setup</CardTitle>
                    <CardDescription>
                      Enter the basic details for your new media experience
                    </CardDescription>
                  </CardHeader>
                  <CardContent>
                    <div className="space-y-6">
                      <div className="space-y-2">
                        <Label htmlFor="campaign-name-ng">Campaign name</Label>
                        <Input id="campaign-name-ng" placeholder="e.g. Summer Sale 2026" value={campaignName} onChange={(e: React.ChangeEvent<HTMLInputElement>) => setCampaignName(e.target.value)} hint="Give your campaign a descriptive name to easily identify it later" />
                      </div>
                      <div className="space-y-2">
                        <Label htmlFor="brand-ng">Brand</Label>
                        <Input dropdown options={brandOptions} value={selectedBrand} onChange={(value: string) => setSelectedBrand(value)} placeholder="Select a brand" />
                        <div className="text-xs text-muted-foreground mt-1">Choose the brand this campaign will advertise for</div>
                      </div>
                    </div>
                    <div className="flex justify-end gap-3 mt-8">
                      <Button variant="ghost">Cancel</Button>
                      <Button disabled={!isSetupComplete} onClick={() => setCurrentStep(1)}>
                        Continue
                      </Button>
                    </div>
                  </CardContent>
                </Card>}

              {/* Step 2: Run time & Budget */}
              {currentStep === 1 && <Card>
                  <CardHeader>
                    <CardTitle className="text-lg">Run time & budget</CardTitle>
                    <CardDescription>
                      Set when your campaign runs and how much you want to spend
                    </CardDescription>
                  </CardHeader>
                  <CardContent>
                    <div className="space-y-6">
                      <div className="space-y-2">
                        <Label>Run time</Label>
                        <DateRangePicker dateRange={dateRange} onDateRangeChange={setDateRange} placeholder="Select start and end date" showPresets />
                        <div className="text-xs text-muted-foreground">
                          Your campaign will automatically start and stop on the selected dates
                        </div>
                      </div>

                      <div className="space-y-2">
                        <Label htmlFor="budget-amount-ng">Total budget</Label>
                        <div className="relative">
                          <span className="absolute left-3 top-1/2 -translate-y-1/2 text-sm text-muted-foreground">€</span>
                          <Input id="budget-amount-ng" type="number" placeholder="e.g. 5000" value={budgetAmount} onChange={(e: React.ChangeEvent<HTMLInputElement>) => setBudgetAmount(e.target.value)} className="pl-7" />
                        </div>
                        <div className="text-xs text-muted-foreground">
                          The maximum total amount for the entire campaign duration
                        </div>
                      </div>

                      {budgetAmount && dateRange?.from && dateRange?.to && <div className="p-3 rounded-lg bg-primary/5 border border-primary/20">
                          <div className="flex items-center justify-between">
                            <span className="text-sm font-medium">Daily average</span>
                            <span className="text-sm font-semibold text-primary">
                              €{(parseFloat(budgetAmount) / (Math.ceil((dateRange.to.getTime() - dateRange.from.getTime()) / (1000 * 60 * 60 * 24)) + 1)).toFixed(2)}/day
                            </span>
                          </div>
                          <p className="text-xs text-muted-foreground mt-1">
                            €{budgetAmount} over {Math.ceil((dateRange.to.getTime() - dateRange.from.getTime()) / (1000 * 60 * 60 * 24)) + 1} days
                          </p>
                        </div>}

                    </div>

                    <div className="flex justify-end gap-3 mt-8">
                      <Button variant="ghost" onClick={() => setCurrentStep(0)}>Back</Button>
                      <Button disabled={!isBudgetComplete} onClick={() => setCurrentStep(2)}>
                        Continue
                      </Button>
                    </div>
                  </CardContent>
                </Card>}

              {/* Step 3: Media plan */}
              {currentStep === 2 && <Card>
                  <CardHeader>
                    <CardTitle className="text-lg">Media plan</CardTitle>
                    <CardDescription>
                      Toggle propositions on or off and choose a preset campaign or start with an empty configuration.
                    </CardDescription>
                  </CardHeader>
                  <CardContent>
                    <div className="space-y-4">
                      {propositions.map(prop => {
                      const selection = propositionSelections[prop.id];
                      const isEnabled = selection !== null && selection !== undefined;
                      const isAiSelected = selection?.mode === 'preset';
                      const isEmptySelected = selection?.mode === 'empty';
                      const IconComponent = prop.icon;
                      return <div key={prop.id} className={cn("rounded-lg border transition-all", isEnabled ? 'border-border' : 'border-border/50')}>
                            {/* Proposition header with toggle */}
                            <div className="flex items-center gap-3 p-4">
                              <div className={cn("w-8 h-8 rounded-lg flex items-center justify-center transition-colors", isEnabled ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground')}>
                                <IconComponent size={16} />
                              </div>
                              <div className="flex-1 min-w-0">
                                <span className={cn("text-sm font-semibold", !isEnabled && 'text-muted-foreground')}>{prop.name}</span>
                                <p className="text-xs text-muted-foreground">{prop.description}</p>
                              </div>
                              <div className="flex items-center gap-3 flex-shrink-0">
                                {isEnabled && <span className="text-xs text-muted-foreground">
                                    {isEmptySelected ? '– reach · – ROAS' : \`\${prop.metrics.reach} reach · \${prop.metrics.roas} ROAS\`}
                                  </span>}
                                <Switch checked={isEnabled} onCheckedChange={(checked: boolean) => {
                              if (checked) {
                                setPropositionSelections(prev => ({
                                  ...prev,
                                  [prop.id]: {
                                    mode: 'preset',
                                    presetId: prop.aiPreset.id
                                  }
                                }));
                              } else {
                                setPropositionSelections(prev => ({
                                  ...prev,
                                  [prop.id]: null
                                }));
                              }
                            }} />
                              </div>
                            </div>

                            {/* Expanded content when enabled */}
                            {isEnabled && <div className="px-4 pb-4">
                                <div className="grid grid-cols-2 gap-3">
                                  {/* AI preset option */}
                                  <button type="button" onClick={() => {
                              setPropositionSelections(prev => ({
                                ...prev,
                                [prop.id]: {
                                  mode: 'preset',
                                  presetId: prop.aiPreset.id
                                }
                              }));
                            }} className={cn("flex flex-col items-start text-left p-4 rounded-lg border-2 transition-all cursor-pointer h-full", isAiSelected ? 'border-primary bg-primary/5 shadow-sm' : 'border-border hover:border-primary/30')}>
                                    <div className="flex items-center gap-2 mb-2">
                                      <Sparkles size={14} className="text-primary flex-shrink-0" />
                                      <span className="text-sm font-medium">Automatic</span>
                                      <Badge variant="secondary" className="text-[10px] px-1.5 py-0">AI</Badge>
                                    </div>
                                    <p className="text-xs text-muted-foreground leading-relaxed mb-3">{prop.aiPreset.description}</p>
                                    <div className="flex gap-3 mt-auto">
                                      <span className="text-xs text-muted-foreground">{prop.aiPreset.placements} placements</span>
                                      <span className="text-xs text-muted-foreground">~{prop.aiPreset.estImpressions} imp.</span>
                                    </div>
                                  </button>

                                  {/* Empty campaign option */}
                                  <button type="button" onClick={() => {
                              setPropositionSelections(prev => ({
                                ...prev,
                                [prop.id]: {
                                  mode: 'empty'
                                }
                              }));
                            }} className={cn("flex flex-col items-start text-left p-4 rounded-lg border-2 border-dashed transition-all cursor-pointer h-full", isEmptySelected ? 'border-primary bg-primary/5 shadow-sm' : 'border-muted-foreground/20 hover:border-primary/30')}>
                                    <div className="flex items-center gap-2 mb-2">
                                      <FileText size={14} className="text-muted-foreground flex-shrink-0" />
                                      <span className="text-sm font-medium">Empty campaign</span>
                                    </div>
                                    <p className="text-xs text-muted-foreground leading-relaxed">
                                      Start with a blank {prop.name.toLowerCase()} campaign and configure manually
                                    </p>
                                  </button>
                                </div>
                              </div>}
                          </div>;
                    })}
                    </div>
                    <div className="flex justify-end gap-3 mt-8">
                      <Button variant="ghost" onClick={() => setCurrentStep(1)}>Back</Button>
                      <Button onClick={() => {
                      const name = campaignName || 'New Media Experience';
                      window.location.href = \`/campaigns?new=\${encodeURIComponent(name)}\`;
                    }}>
                        Launch media experience
                      </Button>
                    </div>
                  </CardContent>
                </Card>}

            </div>

            {/* Summary sidebar */}
            <div className="flex flex-col gap-4">
              <CardSummary>
                <CardHeader>
                  <CardSummaryTitle>Summary</CardSummaryTitle>
                </CardHeader>
                <CardSummaryContent>
                  <div className="relative pl-12">
                    {/* Vertical timeline line */}
                    <div className="absolute left-[19px] top-[16px] bottom-[16px] w-px bg-border"></div>

                    <div className="space-y-4">
                      {wizardStepsNoGoalTargeting.map((step, index) => {
                        const status = getStepStatus(step.id, index);
                        const stepValue = getStepValue(step.id);
                        return <div key={step.id} className="relative flex items-start -ml-12">
                            {/* Circle on the timeline */}
                            <div className="w-10 flex justify-center">
                              <div className={\`w-8 h-8 rounded-full flex items-center justify-center text-xs font-medium \${status === 'completed' ? 'bg-primary text-primary-foreground' : status === 'active' ? 'bg-background text-primary border-2 border-primary' : 'bg-background text-muted-foreground border border-border'}\`}>
                                {status === 'completed' ? <Check size={14} /> : index + 1}
                              </div>
                            </div>
                            {/* Step content */}
                            <div className="ml-3 flex-1 min-w-0 pt-1">
                              <button type="button" className={\`text-sm text-left \${status === 'active' || status === 'completed' ? 'font-medium' : 'text-muted-foreground'} \${status === 'completed' ? 'hover:underline cursor-pointer' : ''}\`} onClick={() => {
                              if (status === 'completed') setCurrentStep(index);
                            }} disabled={status !== 'completed'}>
                                {step.label}
                              </button>
                              {status === 'completed' && stepValue ? <div className="text-sm text-muted-foreground mt-0.5">{stepValue}</div> : status === 'active' ? <div className="text-xs text-muted-foreground italic mt-0.5">
                                  {step.id === 'setup' ? 'Not filled in' : step.id === 'budget' ? 'Not configured' : 'Not selected'}
                                </div> : null}
                            </div>
                          </div>;
                      })}
                    </div>
                  </div>
                </CardSummaryContent>
              </CardSummary>
            </div>
          </div>
          </div>
        </AppLayout>
      </MenuContextProvider>;
  },
  parameters: {
    docs: {
      description: {
        story: \`
# Create Media Experience – No Goal & Targeting

A streamlined variant of the Create Media Experience wizard that removes the Goal and Targeting steps. The wizard flows directly from Campaign Setup to Run Time & Budget, and then to Media Plan.

## Steps

1. **Campaign Setup** - Enter the campaign name, select brand, and choose retail products
2. **Run Time & Budget** - Set the campaign schedule and budget with auto-optimization
3. **Media Plan** - Toggle propositions and choose AI presets or empty campaigns

## Differences from Standard Wizard

- **No Campaign Goal step** (Step 2 removed)
- **No Targeting step** (Step 3 removed)
- **3-step wizard** instead of 5 steps
- Metric cards do not include audience-based reach calculations
- Summary sidebar shows only 3 steps

## Use Cases

- Platforms where goal and targeting are pre-configured or managed separately
- Simplified campaign creation workflows
- Quick campaign setup focused on budget and media selection
        \`
      }
    }
  }
}`,...q.parameters?.docs?.source}}};const ba=["GoalSelection","NoGoalTargeting"];export{Y as GoalSelection,q as NoGoalTargeting,ba as __namedExportsOrder,fa as default};
